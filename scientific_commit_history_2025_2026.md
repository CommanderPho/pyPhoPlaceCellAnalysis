# Scientific motivations and analysis history from commits

Analyzed commit window: 2025-06-25 through 2026-06-21, using `git log --since="1 year ago"` on branch `cursor/scientific-commit-history-cb0e`.

Coverage summary:

- Commits read: 1,407.
- Changed-file records scanned: 2,203.
- Keyword-bearing commit messages: 641.
- Text evidence records extracted from Python/Markdown diffs: 26,910.
- Tracked notebooks changed in this window: 0. The repository currently tracks notebooks, but no `*.ipynb` files changed in the one-year commit range, so notebook markdown-cell evidence was not present in this interval.

Method: commits were processed in chronological order. Evidence was extracted from commit subjects/bodies, Python comments/docstrings and keyword-bearing prose, Markdown/plans, and semantic notebook cell deltas where applicable. The table below is a synthesized scientific-history table; representative commits are listed for traceability rather than as an exhaustive hash list.

| Scientific area | Scientific motivations expressed | Ideas written down or tried | Problems solved | Analyses tried | Things ruled out, deprioritized, or left uncertain | Representative evidence |
|---|---|---|---|---|---|---|
| Long/short track remapping and directional place-field structure | Compare long vs short track/context representations, especially directional place fields and peak shifts, in support of publication figures. | Use 2D place-field peaks, color significantly different place-field peaks, build a track-remapping diagram, compute long/short decoder shifts split by LR/RL direction, and prefer maximum peaks over center-of-mass when appropriate. | Improved long/short comparison plotting, robust peak prominence rendering, fixed figure sizes/labels, improved track drawing, and corrected export fields such as `has_considerable_remapping`. | `TrackRemappingDiagramFigure`, `LongShort3DPlacefieldsHelpers`, `get_directional_pf_maximum_peaks_dfs`, decoder peak-map extraction, Fig. 3 point coloring by XdC/cell identity, and publication bidirectional track-remapping diagrams. | Center-of-mass peak summaries were treated cautiously; rows missing long or short values could be dropped; track-colored reference lines were made removable after feedback; early downsampling did not help. | `c7e42d096`, `ffde2f2a7`, `d78d968e7`, `f932b84c9`, `c49fbde64`, `8d72d3cac`, `47531051d`, `7f373d926`; `DirectionalPlacefieldGlobalComputationFunctions.py` comments around directional peak handling. |
| LxC/SxC firing-rate trends and cross-session Figure 2 style summaries | Quantify how LxC and SxC cells differ in lap and replay/PBE firing-rate changes across sessions. | Add linear regression with p-values, plot R correlations, compute participation statistics, and aggregate lap/replay delta metrics across sessions. | Corrected `SpikeRateTrends` by adding directionality support so strongly directional cells were not unfairly penalized; fixed instantaneous firing-rate computations, percent-change calculations, and time-bin over-division. | Cross-session scatter/bar graphs, instantaneous spike-rate groups, lap/replay delta summaries, `ReplayDeltaMinus/Plus` participation ratios, and HDF5 session tables with globally unique neuron IDs. | Spike sampling rate did not matter for instantaneous spike-rate calculations and was removed; `save_FAT_csv` was cancelled as unnecessary; duplicate ACLUs were removed from summary outputs. | `c96174cd0`, `43ad1ac5a`, `4626980fb`, `2aaea58ea`, `37ef2ec6b`, `b68acc0f4`, `1aa2f6cb8`; `AcrossSessionResults.py` design notes. |
| PBE/replay decoding and Figure 4 posterior examples | Produce publication-ready replay/PBE decoded posterior examples and evaluate whether replay posteriors express meaningful track/context structure. | Propagate `most_likely_positions_1D` into FAT/export dataframes, export posterior HDF5 files, compare decoded and measured occupancy, and ask whether directed PBEs need to follow observed occupancy. | Produced good Figure 4 posterior example outputs, fixed labels and layouts, added per-time-bin and per-epoch correctness exports, and made posterior export more robust. | Generalized decoded epoch plotting, measured-vs-decoded occupancy distributions, trial-to-trial reliability exports, individual epoch correctness, qclu-filtered replay exports, combined image layouts, and example posterior heatmaps. | Measured occupancy plotting was skipped/de-emphasized for PBEs in some paths; adding all-time tracks was blocked by GPU/Qt/timing issues; `most_likely_positions_1D` propagation required moving computation after initial attempts failed. | `3f768a62d`, `2fa180ba0`, `c70196385`, `69f39b357`, `5a118a28f`, `9cb4f51ed`, `8ca80194e`, `f628672ca`. |
| Directional/context decoder correctness on laps | Test whether pseudo-2D directional/context decoders recover true track identity and direction during lap epochs. | Add lap ground-truth information, compute per-lap percent-correct context/direction, clamp decoding bins to valid lap durations, and export session-level correctness dataframes. | Fixed erroneous x/y bin handling in two-step position decoding, added `LapDecodingGroundTruth`, aligned lap quantities, added individual epoch correctness, and corrected position error to use true 2D Euclidean error rather than only x error. | Variable-time-bin lap decoding, complete decoded context correctness, percent-correct context tables, per-session performance plots, train/test lap decoding, and time-bin-specific posterior exports. | Hard-coded long/short assumptions were identified as incompatible with Bapun sessions; for Bapun context-only decoding, direction correctness is not meaningful and is set all-true. | `503d0cf19`, `4bb1e04d4`, `3931aaf02`, `b3bd28fe7`, `9883efbc5`, `5ffcdb202`, `b4a6ed926`; `add_laps_groundtruth_information_to_dataframe`. |
| Bapun/non-KDiba two-maze generalization | Extend long/short and directional-decoding infrastructure to Bapun-style two-maze/open-field sessions without KDiba-specific epoch names. | Build a two-context pseudo-2D decoder from maze1/maze2 place fields, evaluate context identity during laps, add Bapun batch helpers, and compute Bapun position-decoding performance. | Replaced long/short-specific epoch lookups with global/session-general variants, handled missing `non_pbes`, fixed time-column aliases, restored/constructed laps where absent, added Bapun rendering helpers, and separated KDiba-specific completion-result fields. | `evaluate_bapun_context_decoder_performance`, Bapun train/test position-decoding error, occupancy/speed maps, combined time-synchronized Bapun decoder windows, and batch rendering/computation runs. | `find_LongShortGlobal_epoch_names()` and long/short lap-groundtruth logic were ruled out for Bapun; `AddTimeIntervals.SessionEpochs` was disabled by default where it did not work for Bapun sessions. | `9d9d12ba7`, `02d7a57db`, `401132bb3`, `354877d82`, `6c79da793`, `1a7ef6d61`, `4bcc55654`, `072462273`; `PendingNotebookCode.py` Bapun design notes. |
| Rank-order, weighted correlation, and replay sequence significance | Show that replay/ripple weighted correlations and rank-order structure exceed shuffled controls. | Parse standalone shuffle outputs, run many shuffles with qclu/firing-rate thresholds, use best decoded direction, and carry start times into weighted-correlation results. | Added robust shuffle filename parsing, handled duplicate score columns, added `epoch_start_t`, reduced memory by optionally avoiding full decoded shuffle result storage, and filtered short replay epochs. | `WCorrShuffle`, rank-order shuffle analysis, ripple weighted correlations, all-template decoding, replay epoch filtering by duration, and comparison to shuffled distributions. | It remained explicitly questioned which epochs were being tested ("Just replay?"); saving entire decoded shuffle results was noted as memory-heavy; very short events were dropped by duration filters. | `9791772bc`, `67dc7f851`, `c4a0c647b`, `ea735b4eb`; `SequenceBasedComputations.py` comments around `WCorrShuffle`. |
| Predictive decoding locality and posterior specificity | Decide whether decoded posteriors are localized, diffuse, multimodal, or meaningfully related to measured/past/future positions. | Define posterior specificity metrics: focality/diffusivity, sharpness/peakiness, modality, spatial distance to measured position, overlap with Gaussian-smoothed position, and matching past/future positions. | Added sparse Gaussian volume representation for large memory savings, rebuilt missing `locality_measures_df` when needed, deferred expensive matching-past/future computations, and made predictive decoding accept more flexible inputs. | `DecodingLocalityMeasures`, `PredictiveDecoding`, normalized posterior outputs, Gaussian overlap, COM/Euclidean distances, prominence-based peak metrics, and `MatchingPastFuturePositionsResult`. | Earth-mover/similar distance measures were marked too slow; segmentation was disabled to address memory errors; requiring a full directional decoder result was relaxed in favor of downsampled decoded epochs. | `1c94f6fda`, `d83bf437a`, `3fb93fc14`, `cbf10dd5a`, `4f5cd44ab`, `a9e739301`; `PredictiveDecodingComputations.py` metric definitions. |
| Temporal sequentiality and spatial sweeps in 2D decoded posteriors | Detect spatial sweeps across subsequent time bins, with the expectation that real effects should remain at coarser temporal resolution while artifacts may wash out. | Use prominence masks, mask overlap, peak-change vectors, change-vector magnitude, and direction-change angles as sequentiality measures; consider momentum-like constraints on real trajectories. | Refactored inline predictive-decoding sequentiality code into a reusable `compute_temporal_sequentiality_measures` static method. | Jaccard mask overlap between adjacent bins, binary dilation, top peak extraction, bin-space and centimeter-space change vectors, and direction-change angle calculations. | Mask overlap alone was flagged as insufficient because a jumpy sequence can be disjoint; dilating an 80 percent prominence mask may be equivalent to using a lower mask percentage. | `refactor_sequentiality_computations_18bcf0d7.plan.md`; `PredictiveDecodingComputations.py` comments around decoded temporal sequentiality. |
| Posterior peak prominence, contouring, and slab quality | Quantify how many meaningful peaks a decoded posterior contains and visualize the spatial footprint of those peaks across epochs/time bins. | Compute peak prominence masks at alpha levels, count peaks, measure contour area, add peak-count layers, render Napari/Silx contour overlays, and score slab quality for all slabs. | Made prominence computation more robust, handled no-peak cases by adding blanks for each alpha, added peak-count and contour display layers, and worked around matplotlib contour reliability issues. | `PeakPromenence`, posterior peak-contour extraction, 3D/Silx/Napari overlays, `PeakPromenenceMetrics.score_slab_quality`, area-to-posterior-peaks dataframe, and all-slab quality scoring. | Proper prominence-mask parameters were left as a TODO; contour computation was identified as expensive; segmentation was disabled when it caused memory pressure. | `feccdc881`, `50b21669c`, `278778dcb`, `5fe5d8854`, `74cfae123`, `cbf10dd5a`; peak-contour and score-all-slabs plan files. |
| Interactive visual analysis and publication export tooling | Make decoded posterior, spike, track, and place-field analyses inspectable and exportable for publication figures. | Build bin-by-bin decoding debuggers, time-synchronized posterior/spike/track views, paged PDF/GIF exports, all-time track exports, and heatmap export metadata. | Fixed SVG export, figure sizing, axes limits, labels, posterior time labels, Bapun plotting, PDF color consistency, and several PyQt/PyVista/Vispy/Silx rendering issues. | `BinByBinDecodingDebugger`, time-synchronized position decoder plotters, comparison 2D posterior viewers, Napari/Silx/PyVista views, wrapped track exports, and posterior heatmap docks. | PyVista sliders were replaced with Qt sliders for reliability; all-time tracks were not viable without GPU/Qt timing support; several older Silx/Vispy implementations were removed or moved aside. | `b3684f1ba`, `151e2c77e`, `f8df80a53`, `fb72ae40a`, `354877d82`, `eb2ef8b4e`, `c3101d540`. |
| Movement-burst, velocity, and heading analysis | Segment behavior into movement bursts/rest and compute movement/head-direction features that may contextualize neural decoding. | Try Bayesian online change-point detection, DBSCAN clustering, speed/acceleration/jerk/angular-velocity/local-variance features, optional GPU use, and heading from smoothed velocity. | Added memory-safe BOCD downsampling/fallback, guards against offline quadratic behavior on large datasets, robust import fallbacks, and corrected heading angle computation using `arctan2`. | Movement-burst detector, speed smoothing, changepoint detection, clustering, local variance features, heading triangle display, and velocity/occupancy maps. | Offline BOCD was disabled for large datasets; low-speed heading jitter required smoothed-velocity handling; package imports are optional/fallback-based. | `MovementBurstDetection.py`, `042de8e3b`, `f29d93f2f`, `d65f85888`; movement-burst and heading comments. |
| Cross-session and batch processing infrastructure for scientific outputs | Keep each session independently computed while enabling cross-session statistics, batch exports, reproducible run scripts, and publication-ready aggregate figures. | Use context-rich generated script names, HDF5 output tables, status files for external monitoring, resume-friendly computation, and compute-only-as-needed batch helpers. | Improved batch logging/status, made generated run scripts include full contexts and parameter settings, fixed hard-coded paths/folders, improved non-KDiba global computations, and separated KDiba-specific completion results. | Across-session result tables, performance-per-session plots, pooled publication exports, batch figure generation, trial-by-trial reliability, and automated rendering/computation passes. | The cross-session module explicitly avoids being a pipeline manager; global ACLUs need globally unique IDs; old dependency systems and redundant KDiba-specific assumptions were removed. | `67dc7f851`, `a7d7506bc`, `3bbe4cacb`, `debfa92e2`, `072462273`, `4bcc55654`; `AcrossSessionResults.py` design comments. |

High-level through-line: the last year of commits shows a progression from long/short KDiba-style place-field and replay figure generation toward more general context-decoding and predictive-decoding machinery. The recurring scientific concerns are: whether decoded posteriors are specific and sequential rather than diffuse/noisy; whether replay/PBE structure exceeds shuffled controls; whether cell groups differ across laps and replay; and whether the same analysis abstractions can survive beyond hard-coded long/short KDiba sessions into Bapun/non-KDiba sessions.
